<h1>Posts</h1>

<% @posts.each do |post| %>
    <div>
        <h3><%= post.title %></h3>
        <% if user_signed_in? %>
            <p>Author: <%= @users.find(post.user_id).username %></p>
        <% end %>
        <p><%= post.body %></p>
    </div>
<% end %>